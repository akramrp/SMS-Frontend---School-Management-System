import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from 'react';import { authService, type Session } from '@/services/auth/authService';
interface AuthContextValue{session:Session|null;loading:boolean;login:(email:string,password:string)=>Promise<void>;logout:()=>Promise<void>;hasPermission:(p:string)=>boolean}const AuthContext=createContext<AuthContextValue|null>(null);
export function AuthProvider({children}:{children:ReactNode}){const[session,setSession]=useState<Session|null>(null);const[loading,setLoading]=useState(true);useEffect(()=>{authService.getCurrentUser().then(setSession).finally(()=>setLoading(false))},[]);const value=useMemo(()=>({session,loading,login:async(e:string,p:string)=>setSession(await authService.login(e,p)),logout:async()=>{await authService.logout();setSession(null)},hasPermission:(p:string)=>session?.permissions.includes(p)??false}),[session,loading]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>}
export const useAuth=()=>{const c=useContext(AuthContext);if(!c) throw new Error('useAuth must be used within AuthProvider');return c};export function Can({permission,children}:{permission:string;children:ReactNode}){return useAuth().hasPermission(permission)?children:null}
